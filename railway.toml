[build]

[deploy]
dockerUser = "root"
startCommand = "sh -c 'mkdir -p /data/.openclaw && node dist/entry.js gateway --port $PORT --bind lan --allow-unconfigured'"
healthcheckPath = "/"
healthcheckTimeout = 300
